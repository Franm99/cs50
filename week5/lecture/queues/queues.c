/**
 * FIFO - First in first out.
 * Items can be "enqueued", i.e., added to a queue.
 * Items can be "dequeued", i.e., removed from a queue.
 */

/** SAMPLE CODE FOR A QUEUE
const int CAPACITY = 50;
typedef struct
{
    person people[CAPACITY];
    int size;
}
queue;
 */
